# 菜谱抽签 App 开发规划文档

## 1. 项目概述

### 1.1 项目名称

暂定：**今日吃什么**

### 1.2 项目定位

一款基于 Flutter 开发的 Android / iOS 菜谱抽签 App。

核心功能：

> 用户管理自己的菜谱 → 创建不同签池 → 点击签筒进行随机抽签 → 获得今天要做的菜。

产品核心不是普通的随机选择器，而是具有真实「抽签」体验的电子签筒。

### 1.3 核心理念

```text
菜谱库
  ↓
签池
  ↓
抽签算法
  ↓
签筒动画
  ↓
抽出签子
  ↓
今日菜谱
  ↓
查看 / 收藏 / 开始烹饪
```

------

# 2. 技术路线

## 2.1 客户端

使用：

- Flutter
- Dart

目标平台：

- Android
- iOS

采用单代码库开发双端。

------

## 2.2 数据存储

第一阶段采用 **Offline-first** 架构。

本地：

- SQLite
- Drift

第一阶段不依赖服务器。

用户的：

- 菜谱
- 签池
- 收藏
- 抽签历史
- 设置

全部保存在本地。

------

## 2.3 后续服务端

当产品需要：

- 登录
- 云同步
- 多设备同步
- 家庭共享
- 云端菜谱
- AI 菜谱解析

再增加：

```text
Flutter
   ↓
REST API
   ↓
后端服务
   ↓
PostgreSQL
   ↓
对象存储
```

后端可以选择：

- Go
- Node.js

------

# 3. 产品核心模块

```text
App
│
├── 首页 / 抽签
│
├── 签池
│
├── 菜谱
│
├── 收藏
│
├── 抽签历史
│
├── 菜谱导入
│
└── 设置
```

底部导航：

```text
┌────────┬────────┬────────┬────────┐
│  抽签  │  签池  │  菜谱  │  我的  │
└────────┴────────┴────────┴────────┘
```

------

# 4. 菜谱系统

## 4.1 菜谱数据结构

菜谱 Recipe：

```text
Recipe
├── id
├── name
├── categories
├── flavors
├── difficulty
├── cookingTime
├── servings
├── ingredients
├── seasonings
├── steps
├── image
├── description
├── tags
├── isFavorite
├── createdAt
└── updatedAt
```

------

## 4.2 菜谱字段

### 菜名

必填。

例如：

```text
鱼香肉丝
```

### 分类

例如：

```text
川菜
家常菜
下饭菜
快手菜
汤羹
早餐
素菜
肉菜
```

允许用户自定义分类。

### 口味

例如：

```text
微辣
中辣
特辣
酸甜
咸鲜
清淡
麻辣
香辣
```

允许多选。

### 难度

```text
★☆☆☆☆
★★☆☆☆
★★★☆☆
★★★★☆
★★★★★
```

### 烹饪时间

单位：

```text
分钟
```

例如：

```text
15 分钟
30 分钟
60 分钟
```

### 份量

例如：

```text
1 人
2 人
3 人
4 人
```

### 食材

结构化保存：

```text
食材名称
数量
单位
备注
```

示例：

```text
猪里脊    250    g
木耳      50     g
胡萝卜    1      根
青椒      1      个
```

### 调料

结构与食材类似：

```text
豆瓣酱
生抽
陈醋
白糖
淀粉
料酒
```

### 步骤

支持多个步骤：

```text
1. 猪肉切丝
2. 腌制肉丝
3. 调制鱼香汁
4. 热锅炒肉
5. 加入配菜
6. 加入调料
7. 翻炒出锅
```

### 图片

支持：

- 相册选择
- 相机拍摄

第一阶段图片保存在本地。

### 简介

例如：

```text
经典川味家常菜，酸甜微辣，非常下饭。
```

### 标签

例如：

```text
下饭
快手
家常
川菜
新手友好
```

------

# 5. 用户添加菜谱

## 5.1 添加方式

支持：

```text
手动添加
文本导入
URL 导入
```

第一阶段优先实现：

```text
手动添加
文本导入
```

URL 导入作为后续功能。

------

## 5.2 必填字段

仅：

```text
菜名
```

其他字段全部可选。

用户可以快速创建：

```text
番茄炒蛋
```

保存后再编辑完善。

------

## 5.3 快速批量添加

支持一次粘贴：

```text
鱼香肉丝
宫保鸡丁
麻婆豆腐
回锅肉
酸菜鱼
水煮肉片
```

自动识别为多个菜谱。

------

# 6. 默认菜谱库

首次安装 App 时提供默认菜谱。

建议第一版：

```text
川菜       30～50 道
家常菜     30～50 道
快手菜     20～30 道
汤羹       15～20 道
早餐       15～20 道
```

总量：

> 100～150 道。

------

# 7. 默认川菜

重点提供常见川菜，例如：

```text
鱼香肉丝
宫保鸡丁
麻婆豆腐
回锅肉
水煮肉片
辣子鸡
酸菜鱼
毛血旺
夫妻肺片
口水鸡
蒜泥白肉
干煸四季豆
干煸肥肠
青椒肉丝
盐煎肉
蚂蚁上树
鱼香茄子
麻辣豆腐
香辣虾
水煮鱼
泡椒凤爪
酸辣土豆丝
虎皮青椒
蒜苗炒肉
樟茶鸭
东坡肘子
甜烧白
咸烧白
粉蒸肉
冷吃兔
```

默认数据应以结构化 Recipe 数据提供。

------

# 8. 菜谱与签池解耦

菜谱和签子不能设计成一对一关系。

正确关系：

```text
Recipe
   │
   ├──── Pool A
   ├──── Pool B
   └──── Pool C
```

例如：

```text
鱼香肉丝
```

可以同时存在于：

```text
川菜
家常菜
下饭菜
今晚吃什么
我的收藏
```

------

# 9. 签池系统

## 9.1 默认签池

首次安装提供：

```text
川菜
家常菜
快手菜
下饭菜
汤羹
早餐
素菜
肉菜
```

------

## 9.2 用户自定义签池

用户可以创建：

```text
今晚吃什么
```

包含：

```text
鱼香肉丝
番茄炒蛋
青椒肉丝
土豆丝
宫保鸡丁
```

其他示例：

```text
月底省钱菜
想吃辣
30 分钟以内
一人食
周末大餐
```

------

## 9.3 签池操作

支持：

```text
创建
编辑
删除
复制
重命名
添加菜谱
删除菜谱
批量添加
批量删除
排序
```

------

# 10. 抽签系统

## 10.1 抽签流程

```text
点击「抽签」
       ↓
读取当前签池
       ↓
获取候选菜谱
       ↓
执行排除规则
       ↓
随机选择结果
       ↓
启动签筒动画
       ↓
抽出签子
       ↓
显示菜名
       ↓
显示菜谱结果
       ↓
写入抽签历史
```

------

# 11. 抽签算法 V1

第一版本采用普通随机算法。

```text
候选集合
    ↓
Random
    ↓
随机 Recipe ID
```

增加：

### 最近结果排除

用户可以设置：

```text
最近 0 次不排除
最近 1 次不重复
最近 3 次不重复
最近 5 次不重复
```

例如：

```text
A B C D E
```

第一次：

```text
C
```

下一次排除：

```text
C
```

------

# 12. 候选不足处理

如果：

```text
签池只有 3 道菜
```

用户设置：

```text
最近 5 次不重复
```

不能返回错误。

算法自动降低排除条件。

最终保证：

> 用户点击抽签后一定可以得到结果。

------

# 13. 抽签算法 V2

后续支持权重。

例如：

```text
鱼香肉丝    权重 3
宫保鸡丁    权重 2
回锅肉      权重 1
```

权重越高，被抽到概率越大。

后续可以允许用户设置：

```text
最喜欢
喜欢
普通
不太想吃
```

自动转换为权重。

------

# 14. 抽签动画

## 14.1 核心要求

动画必须表现为：

> **真实抽签**

而不是：

> 随机按钮 / 转盘 /老虎机。

------

## 14.2 动画流程

```text
用户点击抽签
       ↓
签筒轻微晃动
       ↓
签子开始上下移动
       ↓
签筒剧烈晃动
       ↓
确定目标签子
       ↓
目标签子逐渐升起
       ↓
其他签子停止
       ↓
目标签子弹出
       ↓
显示签子文字
       ↓
结果卡片展开
```

------

# 15. 动画时间轴

```text
0ms
点击

0～300ms
签筒开始晃动

300～1000ms
签子快速上下运动

1000～1500ms
签筒剧烈晃动

1500～1800ms
目标签子开始升起

1800～2300ms
签子逐渐抽出

2300～2500ms
短暂停顿

2500～2800ms
签子弹出

2800ms+
结果展示
```

------

# 16. 算法与动画分离

必须先确定结果，再播放动画。

正确流程：

```text
点击
 ↓
DrawEngine
 ↓
确定：
鱼香肉丝
 ↓
AnimationController
 ↓
播放抽签动画
 ↓
最终抽出：
鱼香肉丝
```

不能让动画过程决定最终结果。

------

# 17. 抽签结果页

展示：

```text
今日一签

鱼香肉丝

川菜 · 家常菜

难度：★★☆☆☆
时间：25 分钟
口味：酸甜微辣

[查看完整菜谱]

[收藏]

[再抽一次]
```

------

# 18. 菜谱详情页

结构：

```text
菜谱图片
      ↓
菜名
      ↓
分类 / 标签
      ↓
难度 / 时间 / 份量
      ↓
简介
      ↓
食材
      ↓
调料
      ↓
步骤
      ↓
加入签池
```

------

# 19. 收藏系统

用户可以收藏菜谱。

```text
Recipe
   ↓
Favorite
```

收藏页面：

```text
我的收藏

鱼香肉丝
麻婆豆腐
宫保鸡丁
酸菜鱼
```

------

# 20. 抽签历史

记录：

```text
recipeId
poolId
drawTime
```

展示：

```text
今天

16:21  鱼香肉丝
12:03  麻婆豆腐

昨天

19:32  回锅肉
12:17  酸菜鱼
```

------

# 21. 菜谱导入

## 21.1 文本导入

优先实现。

用户粘贴：

```text
鱼香肉丝

猪里脊 250g
木耳 50g
胡萝卜 1根

豆瓣酱
生抽
陈醋

1. 肉丝腌制
2. 调汁
3. 热锅炒肉
4. 翻炒出锅
```

App 自动解析：

```text
菜名
食材
调料
步骤
```

然后：

```text
导入预览
     ↓
用户检查
     ↓
用户修改
     ↓
保存
```

------

# 22. URL 菜谱导入

后续支持：

```text
复制菜谱 URL
      ↓
App 接收 URL
      ↓
解析公开页面
      ↓
提取菜谱结构
      ↓
导入预览
      ↓
用户确认
```

需要考虑：

- 页面结构变化
- 反爬机制
- 图片来源
- 内容版权
- 第三方平台服务条款

不设计为全站批量抓取。

------

# 23. 下厨房导入策略

优先考虑：

```text
下厨房
   ↓
用户主动分享菜谱
   ↓
分享至 App
   ↓
接收 URL
   ↓
解析
   ↓
用户确认
   ↓
保存到本地
```

同时支持：

```text
复制文字
   ↓
粘贴到「智能导入」
   ↓
自动解析
```

不进行全站爬取。

------

# 24. 数据导入与导出

第一版建议支持。

```text
设置
 ↓
数据管理
 ├── 导出数据
 └── 导入数据
```

导出格式：

```text
JSON
```

例如：

```text
backup.json
```

包含：

```text
recipes
pools
favorites
history
settings
```

目的：

- 更换手机
- 数据备份
- 数据迁移
- 防止 App 删除导致数据丢失

------

# 25. 本地数据库设计

## Recipe

```text
Recipe
├── id
├── name
├── difficulty
├── cookingTime
├── servings
├── description
├── imagePath
├── isFavorite
├── createdAt
└── updatedAt
```

## Category

```text
Category
├── id
├── name
└── createdAt
```

## Tag

```text
Tag
├── id
└── name
```

## Ingredient

```text
Ingredient
├── id
├── recipeId
├── name
├── amount
├── unit
└── note
```

## Seasoning

```text
Seasoning
├── id
├── recipeId
├── name
├── amount
├── unit
└── note
```

## RecipeStep

```text
RecipeStep
├── id
├── recipeId
├── stepNumber
├── description
└── imagePath
```

## Pool

```text
Pool
├── id
├── name
├── description
├── createdAt
└── updatedAt
```

## PoolRecipe

```text
PoolRecipe
├── poolId
├── recipeId
└── weight
```

## DrawHistory

```text
DrawHistory
├── id
├── poolId
├── recipeId
└── drawTime
```

## Settings

```text
Settings
├── soundEnabled
├── animationEnabled
├── excludeRecentCount
└── theme
```

------

# 26. Flutter 项目结构

```text
lib/
│
├── main.dart
│
├── app/
│   ├── app.dart
│   ├── router.dart
│   └── theme.dart
│
├── core/
│   ├── database/
│   ├── models/
│   ├── services/
│   ├── utils/
│   └── constants/
│
├── features/
│   │
│   ├── draw/
│   │   ├── pages/
│   │   ├── widgets/
│   │   ├── controllers/
│   │   └── services/
│   │
│   ├── recipes/
│   │   ├── pages/
│   │   ├── widgets/
│   │   ├── controllers/
│   │   └── services/
│   │
│   ├── pools/
│   │   ├── pages/
│   │   ├── widgets/
│   │   └── controllers/
│   │
│   ├── favorites/
│   │
│   ├── history/
│   │
│   ├── import_recipe/
│   │
│   └── settings/
│
└── shared/
    ├── widgets/
    ├── extensions/
    └── helpers/
```

------

# 27. 推荐 Flutter 技术栈

```text
Flutter
Dart
Drift
SQLite
Riverpod
GoRouter
Freezed
JSON Serializable
```

动画：

```text
Flutter Animation
AnimationController
Tween
CurvedAnimation
CustomPainter
```

复杂动画可以后续引入：

```text
Rive
```

或：

```text
Lottie
```

------

# 28. 状态管理

使用：

> Riverpod

主要状态：

```text
RecipeState
PoolState
DrawState
FavoriteState
HistoryState
SettingsState
```

抽签状态：

```text
idle
 ↓
drawing
 ↓
revealing
 ↓
completed
```

------

# 29. 核心 DrawEngine

抽签引擎负责：

```text
获取签池
 ↓
获取菜谱
 ↓
过滤无效数据
 ↓
读取历史
 ↓
应用排除规则
 ↓
计算权重
 ↓
随机选择
 ↓
返回 DrawResult
```

不要让 UI 直接实现随机逻辑。

------

# 30. DrawResult

统一抽签结果：

```text
DrawResult
├── recipeId
├── poolId
├── recipeName
├── timestamp
└── animationSeed
```

其中：

```text
animationSeed
```

用于保证动画表现具有一定随机性，同时能够进行调试和复现。

------

# 31. UI 设计方向

整体风格：

> 温暖、轻量、具有传统签筒元素，但不能过度古风。

推荐视觉元素：

```text
米白
木色
竹签
纸张
轻微红色印章
圆角卡片
柔和阴影
```

避免：

```text
过度国风
复杂纹理
大量金色
繁琐装饰
游戏化 UI
```

核心视觉焦点：

```text
签筒
签子
菜谱结果
```

------

# 32. 首页布局

```text
┌──────────────────────────┐
│ 今天吃什么？             │
│                          │
│ 当前签池：川菜       ▼   │
│                          │
│        ╭──────╮          │
│       ╱  签筒  ╲         │
│      │  ╱╱╱╲  │         │
│      │ ╱╱╱╱╲ │         │
│       ╲______╱           │
│                          │
│       [ 抽一签 ]         │
│                          │
│ 最近：鱼香肉丝           │
└──────────────────────────┘
```

------

# 33. 设置

```text
设置

抽签
├── 动画效果
├── 抽签音效
└── 最近结果排除

外观
├── 跟随系统
├── 浅色
└── 深色

数据
├── 导入数据
├── 导出数据
└── 清空数据

关于
├── 版本
└── 开源许可
```

------

# 34. MVP 第一版本

必须完成：

```text
□ Flutter Android / iOS 项目
□ SQLite 本地数据库
□ 默认菜谱
□ 默认川菜签池
□ 菜谱列表
□ 菜谱详情
□ 搜索菜谱
□ 添加菜谱
□ 编辑菜谱
□ 删除菜谱
□ 创建签池
□ 编辑签池
□ 删除签池
□ 添加菜谱到签池
□ 抽签算法
□ 最近结果排除
□ 签筒动画
□ 抽签结果
□ 收藏
□ 抽签历史
□ 数据导入
□ 数据导出
□ 基础设置
```

------

# 35. 第二阶段

```text
□ 批量添加菜谱
□ 图片管理
□ 相机拍摄
□ 文本智能导入
□ 菜谱 URL 导入
□ 分享接收 URL
□ 更丰富的动画
□ 抽签音效
□ 深色模式
□ 权重抽签
□ 自定义标签
□ 自定义分类
```

------

# 36. 第三阶段

```text
□ 用户账号
□ 云同步
□ 多设备同步
□ 云端图片
□ 家庭共享
□ 菜谱分享
□ 一周菜单
□ 冰箱食材模式
□ 预算模式
□ 营养信息
□ AI 菜谱解析
□ AI 菜谱生成
□ AI 菜单规划
```

------

# 37. 暂不开发

```text
□ 社区
□ 评论
□ 点赞
□ 关注
□ 私信
□ 商城
□ 直播
□ 广告系统
□ 复杂社交系统
```

------

# 38. 开发阶段

## Phase 1：项目基础

```text
Flutter
 ↓
项目初始化
 ↓
路由
 ↓
主题
 ↓
底部导航
 ↓
SQLite
```

## Phase 2：菜谱

```text
Recipe Model
 ↓
数据库
 ↓
菜谱列表
 ↓
详情
 ↓
新增
 ↓
编辑
 ↓
删除
 ↓
搜索
```

## Phase 3：签池

```text
Pool Model
 ↓
默认签池
 ↓
自定义签池
 ↓
菜谱加入 / 移除
```

## Phase 4：抽签引擎

```text
Pool
 ↓
Candidate
 ↓
Filter
 ↓
Random
 ↓
DrawResult
```

## Phase 5：动画

```text
DrawResult
 ↓
AnimationController
 ↓
签筒摇晃
 ↓
签子运动
 ↓
目标签子升起
 ↓
签子抽出
 ↓
结果展示
```

## Phase 6：收藏与历史

```text
Favorite
DrawHistory
```

## Phase 7：导入导出

```text
JSON Export
JSON Import
Text Import
```

## Phase 8：测试与发布

```text
Android
 ↓
功能测试
 ↓
性能测试
 ↓
动画测试
 ↓
数据迁移测试
 ↓
正式发布
```

------

# 39. 开发原则

## 原则一：Offline-first

核心功能不依赖网络。

```text
没有网络
    ↓
依然可以
    ↓
查看菜谱
创建菜谱
创建签池
抽签
收藏
查看历史
```

------

## 原则二：数据属于用户

必须支持：

```text
导出
备份
恢复
迁移
```

------

## 原则三：菜谱与签池解耦

不要把菜谱和签池绑定。

------

## 原则四：算法与动画解耦

```text
DrawEngine
```

只负责：

> 抽什么。

```text
AnimationController
```

只负责：

> 怎么抽出来。

------

## 原则五：第一版不做后端

先把：

```text
菜谱
+
签池
+
抽签
+
动画
```

做完整。

------

# 40. 最终产品架构

```text
                         Flutter App
                              │
          ┌───────────────────┼───────────────────┐
          │                   │                   │
          ▼                   ▼                   ▼
       菜谱系统              签池系统            用户系统
          │                   │                   │
          │                   │              收藏 / 历史
          │                   │
          └──────────┬────────┘
                     │
                     ▼
                DrawEngine
                     │
                     ▼
              Random Selection
                     │
                     ▼
             AnimationController
                     │
                     ▼
                 签筒动画
                     │
                     ▼
                 抽签结果
                     │
             ┌───────┼───────┐
             ▼       ▼       ▼
           菜谱     收藏     历史
```

------

# 41. 长期产品路线

```text
                    V1
                     │
                     ▼
              本地菜谱抽签 App
                     │
          ┌──────────┴──────────┐
          ↓                     ↓
       用户菜谱                默认菜谱
          │                     │
          └──────────┬──────────┘
                     ↓
                    签池
                     ↓
                   抽签
                     ↓
                  菜谱结果
                     │
                     ▼
                    V2
                     │
          ┌──────────┼──────────┐
          ↓          ↓          ↓
       智能导入    URL导入     权重抽签
          │
          ▼
                    V3
                     │
          ┌──────────┼──────────┐
          ↓          ↓          ↓
        云同步      AI         家庭共享
                     │
          ┌──────────┼──────────┐
          ↓          ↓          ↓
       AI识别      AI菜单      食材推荐
                     │
                     ▼
              完整个人饮食助手
```

------

# 42. V1 最终目标

第一版不追求功能数量。

只需要把下面这条链路做到完整：

```text
打开 App
   ↓
看到签筒
   ↓
选择签池
   ↓
点击「抽一签」
   ↓
签筒晃动
   ↓
签子随机运动
   ↓
一根签子升起
   ↓
签子被抽出
   ↓
显示「鱼香肉丝」
   ↓
查看菜谱
   ↓
收藏 / 再抽
   ↓
记录抽签历史
```

**产品第一阶段的核心验收标准：**

> 用户打开 App 后，不需要思考如何使用，能够在 5 秒内开始抽签；抽签过程必须有明显的「抽签感」，而不是普通随机数生成器；用户可以随时建立自己的菜谱和签池，并且所有核心数据在无网络状态下正常工作。