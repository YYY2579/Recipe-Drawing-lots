# 本机验证清单（今日吃什么 · 菜谱随机抽签）

> 适用：已安装 Flutter SDK（≥3.22 / Dart ≥3.4）的机器。
> 沙箱侧已完成的静态校验：**141 道菜谱 / 6 个签池 / 0 条悬空引用 / 无重复 id / 默认池 `p-sichuan` 存在 / 两个种子 JSON 均合法**。
> 沙箱无 Dart SDK，无法替你编译，下面是你本机要执行的步骤。

---

## 0. 前置条件（先确认）

| 项 | 命令 | 期望 |
| --- | --- | --- |
| Flutter | `flutter --version` | Flutter ≥3.22.0，Dart ≥3.4.0 |
| 设备/模拟器 | `flutter devices` | 至少 1 台可用（iOS 模拟器 / Android 模拟器 / 真机） |

若在 macOS 跑 iOS 模拟器：先开 `Xcode → 打开模拟器`（或 `open -a Simulator`），再 `flutter run`。
若跑 Android：先启动一个 AVD。

---

## 1. 一条命令跑通（推荐）

在仓库根目录 `/Users/yyy/Desktop/菜谱随机抽签` 执行：

```bash
bash scripts/bootstrap.sh
```

`bootstrap.sh` 会依次做：
1. 用 `flutter create --platforms=android,ios` 生成 `android/ ios/ .metadata .gitignore`（**不覆盖**已有的 `pubspec.yaml` 与 `lib/`）；
2. `flutter pub get` 装依赖；
3. `dart run build_runner build --delete-conflicting-outputs` 生成 Drift 的 `app_database.g.dart`；
4. 提示你 `flutter run`。

> bootstrap.sh 只生成平台壳、不启动 App。最后一步仍需你手动 `flutter run`。

---

## 2. 分步执行（想看清每段输出时）

```bash
cd /Users/yyy/Desktop/菜谱随机抽签

# ① 生成平台目录（仅首次，已生成过可跳过）
flutter create --platforms=android,ios .

# ② 拉依赖
flutter pub get

# ③ 生成 Drift 代码（关键：否则会报找不到 app_database.g.dart）
dart run build_runner build --delete-conflicting-outputs

# ④ 运行
flutter run
```

---

## 3. 常见报错速查

| 报错关键字 | 原因 | 修复 |
| --- | --- | --- |
| `Target of URI hasn't been generated: ...app_database.g.dart` | 没跑 build_runner | 执行第 ③ 步 |
| `Couldn't find getter 'xxx' in Tables` / `No member named 'copyWith'` | `.g.dart` 过期或不一致 | `dart run build_runner build --delete-conflicting-outputs` 重生成 |
| `The current Dart SDK version is 3.x.x, but ... requires ^3.4.0` | SDK 太旧 | 升级 Flutter：`flutter upgrade` |
| `Because what_to_eat requires ... which ... doesn't support` | 依赖与 SDK 不兼容 | `flutter pub get` 重试；仍不行贴完整报错 |
| `A value of type 'X' can't be assigned to ...`（编译期类型错误） | 手写 Provider/Dart 类型问题 | 把红色报错整段贴回，精确定位文件:行号 |
| `Undefined name '...'` / `The named parameter '...' isn't defined` | 漏 import 或字段名拼错 | 同上，贴报错 |
| `WARNING: ...` 黄色警告 | 多为 lint，不影响运行 | 可忽略，先跑起来 |
| 启动后白屏 / 首页无菜谱 | 种子未写入（Recipes 表空） | 确认 `assets/seed/` 已进包；卸载重装 App 再开；贴运行日志 |
| `Unable to locate a development device` | 没有可用设备 | 先开模拟器/真机，再 `flutter run` |

> 黄色 `WARNING`/`INFO` 一般**不影响运行**，优先让 App 起来。红色 `error:` 才需要修。

---

## 4. 功能回归清单（App 起来后逐项点）

1. **首页抽签**：选池（默认川菜）→ 点签筒 → 出结果卡（含菜名/分类/难度/时长）。
2. **结果 → 详情**：结果页进 `/recipe/:id`，看食材/调料/步骤三块。
3. **菜谱库**：`/recipes` 搜索 + 9 分类筛选；点进去看详情；右上 + 新建一道并保存；在某条上删除。
4. **签池**：`/pools` 列表与计数；进 `/pools/:id` 增删池内菜谱；新建/删除一个签池。
5. **收藏 / 历史**：结果或详情点收藏 → `/favorites` 可见；抽签若干次 → `/history` 列表；清空历史。
6. **设置 / 我的**：`/settings` 切音效/动画、拖「最近不重复」并保存（重开生效）；`/profile` 入口可达。

---

## 5. 报错怎么贴给我

直接把 `flutter run` 的**红色报错整段**贴回来即可，最好带上 `文件:行号`。
如果只是某个功能点了没反应 / 闪退，贴对应运行日志（带 `flutter: ` 或 exception 堆栈）。

我会据此精确定位并改源码（沙箱写盘，你本地重跑）。

---

## 6. 仍待补（验证通过后可选）

- 详情页配图打磨（当前 `imagePath` 为 null，用占位图）
- 签池内菜谱排序（PoolRecipes 已有 `weight` 字段，未接入 UI）
- 开源数据集扩充（现 141 道，可再扩）
- 文本 / URL 智能导入菜谱
- JSON 导入 / 导出备份
